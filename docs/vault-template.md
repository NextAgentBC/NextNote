# nextNote AI Soul — Vault Template

The canonical template ships inside the app bundle at
[nextNote/Resources/vault-template/](../nextNote/Resources/vault-template/).

`LibrarySetupView → Use AI Soul Preset` copies that directory into the user's
chosen Notes root. Contents: full `CLAUDE.md` / `Soul.md` / `MEMORY.md` /
templates / 21 skills / prompts — Phase A deliverable per [AI_PLAN.md](../AI_PLAN.md).

Edit the template in `nextNote/Resources/vault-template/` — single source of
truth, auto-bundled with the app via xcodegen `sources:`.
