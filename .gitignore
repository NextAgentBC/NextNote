# Xcode
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/
*.xcuserstate

# xcodegen regenerates these locally
nextNote.xcodeproj/

# Build output
build.nosync/
build/
dist/

# SwiftPM
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# AppCode / IntelliJ
.idea/

# Logs / scratch
*.log
/tmp/

# Local overrides
.env
.env.local
AGENTS.md
.claude/

# Local screen recordings / throwaway demo captures — the committed demo
# assets (alice-in-wonderland.epub, sample-clip.mp4) stay; anything else
# is the author's local recording that shouldn't bloat the repo.
demo/*.mov
demo/nextnote-demo.mp4
