<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Non-sandboxed desktop app with hardened runtime. Sandbox is off so
      nextNote can spawn Homebrew CLI tools (yt-dlp, ffmpeg, ollama)
      from their fixed install paths without making the user re-grant
      each binary via NSOpenPanel on every fresh install. Hardened
      runtime + notarization still apply.
    -->
    <key>com.apple.security.app-sandbox</key>
    <false/>
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Hardened runtime extras for MLX / SPM dynamic libs / WKWebView JIT. -->
    <!-- JIT + unsigned executable memory: MLX inference kernels. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- SwiftPM dylibs aren't signed with our Team ID; allow loading. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
